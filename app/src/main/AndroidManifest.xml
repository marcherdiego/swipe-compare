<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.marcherdiego.swipecompare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwipeCompare">
        <activity android:name="com.github.marcherdiego.swipecompare.ui.activities.HorizontalSwipeActivity"/>
        <activity android:name="com.github.marcherdiego.swipecompare.ui.activities.VerticalSwipeActivity"/>
        <activity android:name="com.github.marcherdiego.swipecompare.ui.activities.CrosshairSwipeActivity"/>
        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
